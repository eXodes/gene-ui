export enum APPEARANCE {
  DEFAULT = "default",
  PRIMARY = "primary",
  ATTENTION = "attention",
  SUCCESS = "success",
  WARNING = "warning",
  DANGER = "danger",
}

export enum SIZE {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}

export enum VARIANT {
  FILL = "fill",
  CLEAR = "clear",
}
